#include <stdio.h>

int main () {
    int var = 10; // atribuição, o igual (=) no C, você atribui algo a variavel

    printf ("%d\n", var);

    var = 20; // atribuição

    printf("%d\n", var);

    return 0;

}